{
  "name": "@webexp/functions",
  "version": "0.0.0",
  "main": "lib/index.js",
  "repository": "git@github.com:Dabolus/web-experiments.git",
  "author": "Giorgio Garasto <giorgio@garasto.me>",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "run-p watch:esbuild watch:firebase",
    "watch:esbuild": "yarn esbuild --watch --sourcemap=inline src/index.ts",
    "watch:firebase": "wait-on lib/index.js && firebase emulators:start",
    "build": "yarn esbuild --minify --sourcemap src/index.ts",
    "esbuild": "esbuild --platform=node --target=node16 --outdir=lib --format=esm --bundle --external:firebase-functions --external:firebase-admin"
  },
  "dependencies": {
    "firebase-admin": "^11.1.0",
    "firebase-functions": "^4.0.0-rc.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.11",
    "firebase-functions-test": "^2.4.0",
    "npm-run-all": "^4.1.5",
    "wait-on": "^6.0.1"
  },
  "engines": {
    "node": "16"
  }
}
